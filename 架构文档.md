# 项目架构文档

> 个人技术博客 + 在线工具箱，VuePress 2.0 + vuepress-theme-plume 静态站。
> 适用读者：后续需要新增工具页面、调整主题、新增 markdown 内容的开发者。

---

## 1. 总览

| 维度 | 选型 | 备注 |
|---|---|---|
| 静态站生成 | **VuePress 2.0-rc.26** | 配置 + Markdown + Vue SFC |
| 主题 | **vuepress-theme-plume 1.0.0-rc.185** | 提供博客/导航/搜索/Markdown 增强等 |
| 打包器 | **Vite** (`@vuepress/bundler-vite`) | 在 `config.js` 中通过 `viteBundler` 启用 |
| 前端框架 | **Vue 3.5**（`<script setup>` + TS） | 所有自定义组件 |
| 类型 | **TypeScript 5.9** | 仅类型，组件也是 `.vue` + `<script setup lang="ts">` |
| 数据可视化 | **echarts 6** | 仅在需要的工具中按需引入 |
| 加密 | **sm4-crypto**、**blueimp-md5** | 加密工具 |
| 部署 | **GitHub Actions → gh-pages** | `push main` 自动构建发布 |

线上域名：`https://blog.minjor.com`（自定义域，在 `docs/.vuepress/public/CNAME`）。Git 仓库本身部署在 `minjorx.github.io`。

---

## 2. 目录结构

```text
minjorx.github.io/
├── .github/workflows/deploy.yml          # CI：build → 推 gh-pages
├── docs/
│   ├── README.md
│   ├── blog/                              # 博客集合（collection: blog, type: post）
│   │   ├── 🚀 开发相关/
│   │   ├── 📝 博客/
│   │   ├── 🗃️ 临时目录/
│   │   ├── 模板/
│   │   └── preview/                       # 自定义组件示例
│   ├── tools/                             # 网页工具入口集合
│   │   ├── README.md                      # 工具总览页
│   │   ├── accounting.md                  # → 嵌入 <AccountingComponent/>
│   │   ├── stat.md                        # → 嵌入 <StatComponent/>
│   │   ├── sync.md                        # → 嵌入 <SyncComponent/>
│   │   ├── ip-lookup.md                   # → 嵌入 <IPLookupComponent/>
│   │   ├── crypto.md                      # → 嵌入 <CryptoToolComponent/>
│   │   ├── time.md                        # → 嵌入 <TimeToolComponent/>（ClientOnly）
│   │   └── bpm.md                         # → 嵌入 <BpmToolComponent/>（ClientOnly）
│   └── .vuepress/
│       ├── config.js                      # 主配置：站点/插件/bundler
│       ├── plume.config.js                # 主题配置：logo/social/navbar/collections
│       ├── navbar.js                      # 顶导
│       ├── collections.js                 # 文档集合（仅启用 blog）
│       ├── client.js                      # 客户端增强：注册全局自定义组件
│       ├── public/                        # 静态资源原样拷贝到 dist 根
│       │   ├── CNAME
│       │   ├── favicon.ico
│       │   └── sources/                   # 业务图片源
│       ├── theme/                         # 主题扩展（被 plume 加载）
│       │   ├── components/                # 自定义 Vue 组件
│       │   │   ├── Custom.vue
│       │   │   ├── accounting/{Accounting,Stat,Sync}.vue
│       │   │   ├── bpm-tool/BpmTool.vue
│       │   │   ├── crypto-tool/CryptoTool.vue
│       │   │   ├── ip-lookup/IPLookup.vue
│       │   │   └── time-tool/TimeTool.vue
│       │   └── styles/custom.css          # 全局 CSS 变量 & 通用样式（暂未在 client.js 启用）
│       ├── components/                    # 顶层组件覆盖（MyFooter.vue）
│       └── util/                          # 内部工具
├── minjorx.github.io.code-workspace       # 工作区代码根配置
├── package.json                           # 脚本 & 依赖
├── README.md / README.zh-CN.md
└── 项目规范.md                            # 样式使用规范
```

> `node_modules`、`docs/.vuepress/.cache`、`docs/.vuepress/.temp`、`docs/.vuepress/dist` 均在 `.gitignore` 中。

---

## 3. 运行时结构（关键链路）

```text
                          push main
                              │
                              ▼
              ┌── .github/workflows/deploy.yml ──┐
              │  ubuntu-latest                   │
              │  ├─ actions/checkout@v4          │
              │  ├─ setup-node@v4 (node 22)      │
              │  ├─ npm ci                       │
              │  ├─ npm run docs:build           │  ← docs:build = vuepress build docs --clean-cache --clean-temp
              │  └─ crazy-max/ghaction-github-pages@v4
              │       target_branch: gh-pages    │
              │       build_dir: docs/.vuepress/dist
              └───────────────────────────────────┘
                              │
                              ▼
                       gh-pages 分支
                       （GitHub Pages 托管，自定义域 blog.minjor.com）
```

---

## 4. 配置分层

### 4.1 `docs/.vuepress/config.js`（主配置）
- 站点信息：`title / description / lang / base: "/" / hostname: https://blog.minjor.com`
- `bundler: viteBundler({ viteOptions: { server: { allowedHosts: ['minjor.51vip.biz'] } } })` —— 本地 `docs:dev` 允许通过该域名访问
- `theme: plumeTheme({ … })` —— 大量 Markdown 增强（figure、lazyload、katex、chartjs/echarts/mermaid 默认未开）
- `alias` 把内置 `VPFooter` 重写到自定义 `MyFooter.vue`：`@theme/VPFooter.vue → ./components/MyFooter.vue`

### 4.2 `docs/.vuepress/plume.config.js`（主题配置）
- `logo / profile / social`：站点头部
- 引入 `navbar.js` 与 `collections.js`

### 4.3 `docs/.vuepress/navbar.js`
```js
首页 | 博客 | 标签 | 分类 | 归档 | 网站工具 (/tools/README.md)
```
"网站工具" 入口指向 `tools/README.md`。

### 4.4 `docs/.vuepress/collections.js`
仅定义了一个 `blog` collection（`type: 'post'`，目录 `docs/blog/`），自动生成博客列表/标签/分类/归档页。`demoDoc` 已注释删除。

### 4.5 `docs/.vuepress/client.js`（**自定义组件注册入口**）
```js
app.component('XxxCustomComponent', XxxComponent)
```
当前注册了 8 个：`CustomComponent` + 7 个工具组件。这是新增自定义组件**必须改**的地方。

---

## 5. "自定义页面" 是怎么搭起来的

### 5.1 模式一：Markdown + Vue SFC（**当前所有工具页采用**）

每个工具由「一个 `.md` 入口 + 一个 `.vue` 组件」构成：

**入口 `.md`**（以 `docs/tools/accounting.md` 为例）：
```markdown
---
home: true
title: 记账本
navbar: false
config:
  - type: custom
createTime: 2026/01/11 13:17:20
---
<AccountingComponent />
```

要点：
- `home: true` + `config: [{ type: custom }]` —— plume 的"自定义首页"配置，让页面以组件为主体（去掉默认文档布局）
- `navbar: false` —— 隐藏顶部导航，工具页通常更聚焦
- body 里只放一个组件标签

**组件 `.vue`**（在 `docs/.vuepress/theme/components/<dir>/Xxx.vue`）：
- `<script setup lang="ts">` + Vue 3 composition API
- 内含自己的状态、数据持久化（目前用 localStorage）、可选的 echarts 图表
- 主题样式由组件自带 `<style scoped>` 提供，**不要写全局样式污染**（参 `项目规范.md`）

**注册**（`docs/.vuepress/client.js`）：
```js
import AccountingComponent from "./theme/components/accounting/Accounting.vue";
…
app.component("AccountingComponent", AccountingComponent);
```

> ⚠️ 顺序：**先在 client.js 注册 → 再在 markdown 里写 `<XxxComponent />`**，否则 dev/build 会报组件未定义。

### 5.2 模式二：客户端包裹（`<ClientOnly>`）

时间工具与 BPM 工具需要直接操作 `window`/浏览器 API，用 `<ClientOnly>` 包裹避免 SSR 报错：

```markdown
<ClientOnly>
<TimeToolComponent />
</ClientOnly>
```

### 5.3 静态资源访问

`docs/.vuepress/public/` 整个目录**原样拷贝**到 `docs/.vuepress/dist/` 根目录。
访问路径 = `public/` 下的相对路径：

| 源文件 | 部署后 URL |
|---|---|
| `docs/.vuepress/public/favicon.ico` | `https://blog.minjor.com/favicon.ico` |
| `docs/.vuepress/public/CNAME` | （构建期被 GitHub Pages 用于域名映射，不直接对外） |
| `docs/.vuepress/public/sources/AI.png` | `https://blog.minjor.com/sources/AI.png` |

> 不要自己再加 `resources/` 目录——plume/VuePress 的约定是 `public/`。

### 5.4 组件别名覆盖（`alias`）

`config.js` 里的 `alias` 可把主题内置组件替换成本地实现：
```js
alias: { '@theme/VPFooter.vue': path.resolve(__dirname, './components/MyFooter.vue') }
```
已用于自定义页脚。也可用于替换 `VPHome`、`VPSidebar` 等（参 plume 文档）。

---

## 6. 已落地的自定义页面清单

| Markdown 入口 | 组件 | 行数 | 关键依赖 | 客户端 |
|---|---|---|---|---|
| `tools/accounting.md` | `accounting/Accounting.vue` | 1062 | localStorage | 否 |
| `tools/stat.md` | `accounting/Stat.vue` | 1506 | echarts | 否 |
| `tools/sync.md` | `accounting/Sync.vue` | 541 | localStorage | 否 |
| `tools/ip-lookup.md` | `ip-lookup/IPLookup.vue` | 428 | — | 否 |
| `tools/crypto.md` | `crypto-tool/CryptoTool.vue` | 855 | sm4-crypto、blueimp-md5 | 否 |
| `tools/time.md` | `time-tool/TimeTool.vue` | 448 | — | **是**（ClientOnly） |
| `tools/bpm.md` | `bpm-tool/BpmTool.vue` | 956 | — | **是**（ClientOnly） |
| `blog/preview/custom-component.example.md` | `Custom.vue` | 11 | — | 否（示例） |

---

## 7. 新增需求的标准流程

### 7.1 新增一个工具页

以"加一个汇率换算工具"为例：

1. **新建组件**
   `docs/.vuepress/theme/components/exchange-tool/ExchangeTool.vue`
   - `<script setup lang="ts">`，自带 `<style scoped>`
   - 需要 `window` 的就放 `onMounted` / `ClientOnly` 包
2. **注册到 client.js**（按字母顺序插入即可，便于 review）
   ```js
   import ExchangeToolComponent from "./theme/components/exchange-tool/ExchangeTool.vue";
   …
   app.component("ExchangeToolComponent", ExchangeToolComponent);
   ```
3. **新建 markdown 入口** `docs/tools/exchange.md`
   ```markdown
   ---
   home: true
   title: 汇率换算
   navbar: false
   config:
     - type: custom
   createTime: 2026/09/22 09:30:00
   ---
   <ExchangeToolComponent />
   ```
4. **在 `tools/README.md` 增加条目**
   ```
   - **<a href="/tools/exchange">汇率换算</a>** - 一句话功能描述
   ```
5. **如需新依赖**：先 `npm i <pkg>`，让 `package.json` 锁版本；提交时**只动** `package.json + package-lock.json` + 组件文件
6. **提交推送**
   - `git add` 对应文件
   - `git commit -m "feat: 新增汇率换算工具"`
   - `git push origin main` —— Actions 自动 build & deploy

### 7.2 新增一篇博客文章

1. 在 `docs/blog/<分类目录>/xxx.md` 下新增文件
2. frontmatter 至少要有 `title`；`createTime` 主题会自动按 git log 补齐（plume 的 `autoFrontmatter.createTime` 已开启）
3. 永久链接：`permalink` 可写死，不写则根据文件名生成
4. 文件落点自动被 `blog` collection 收集，标签/分类/归档页自动出现
5. 推送即可

### 7.3 修改主题色 / 通用样式

- 全站 CSS 变量集中在 `docs/.vuepress/theme/styles/custom.css`
- 当前 **该 CSS 未在 `client.js` 中 import**（代码被注释）：
  ```js
  // import './theme/styles/custom.css'
  ```
- 修改或新增样式后，记得启用 import，否则不生效
- ⚠️ 严格遵守 `项目规范.md`：业务组件**只写结构不写样式**，样式集中在 `theme/styles/`

### 7.4 替换主题内置组件

通过 `config.js` 的 `alias`，例如替换首页：
```js
import { path } from 'vuepress/utils';
const __dirname = getDirname(import.meta.url);
export default defineUserConfig({
  alias: {
    '@theme/VPHome.vue': path.resolve(__dirname, './theme/components/MyHome.vue'),
  },
});
```

### 7.5 调整 Markdown 能力

集中在 `config.js` 的 `theme.markdown`：
- `image`：figure、lazyload、size、mark
- `math: { type: 'katex' }`
- 已禁用：`chartjs / echarts / mermaid / flowchart` —— 想要图表直接在组件里用 echarts 更灵活
- 启用新能力：翻 plume 文档 → 在此对象加 key

---

## 8. 易踩的坑

| 现象 | 原因 / 解决 |
|---|---|
| `<XxxComponent />` 报 "Component not registered" | 没在 `client.js` 里 `app.component(...)` 注册 |
| 工具页样式没应用 / 报错 | 检查 markdown frontmatter 是否漏 `home: true` + `config: [{ type: custom }]` |
| 浏览器 API 报错（`window is not defined`） | 组件需要 `<ClientOnly>` 包裹（参考 `time.md` / `bpm.md`） |
| 新图片访问 404 | 放在 `public/` 下；路径 = `public/` 起的相对路径；必须重新 build |
| `base` 不对导致资源 404 | 当前 `base: '/'`；若迁仓库名部署，需改为 `'/<repo>/'` |
| 全局样式不生效 | `custom.css` 未在 `client.js` 启用 import |
| 想加 `public/sources/foo.png` 在页面引用 | 用绝对路径 `/sources/foo.png`（`base: '/'`） |
| Vite dev 提示 "blocked host" | `viteOptions.server.allowedHosts` 加白名单，或本地用 `localhost` |

---

## 9. 本地开发命令速查

| 命令 | 作用 |
|---|---|
| `npm run docs:dev` | 启动本地 dev（端口 5173，可通过 `minjor.51vip.biz` 访问） |
| `npm run docs:dev-clean` | 清缓存重启 |
| `npm run docs:build` | 生产构建到 `docs/.vuepress/dist` |
| `npm run docs:preview` | 用 http-server 在本地预览 dist |
| `npm run vp-update` | 升级 vuepress 与主题 |

> ⚠️ `docs/.vuepress/dist/` 不入库；正式部署只走 Actions。

---

## 10. 后续可演进的方向（可选）

- 把"工具"做成独立 page bundle：每个工具组件 `defineAsyncComponent` + 路由级懒加载（plume 已支持 page chunks）
- 给工具页加 PWA / 离线缓存（`@vuepress/plugin-pwa`）
- 把业务工具从主题里剥离到一个独立 npm workspace（`packages/tools`），主题只做壳
- 抽出 `theme/styles/custom.css` 的变量到 `theme-plume` 的 `themeConfig` 字段（避免裸 CSS 变量）