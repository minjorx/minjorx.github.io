# 新增工具开发指南

> 在本仓库新增一个工具页面（网页工具 / 在线小功能）的完整步骤与约定。
> 适用读者：之后想加新工具的开发者。
> 配套阅读：[架构文档.md](架构文档.md)、[项目规范.md](项目规范.md)

---

## 1. 工具页面的全貌

工具页面 = **3 个文件** + **1 处入口更新**：

```text
docs/
├── .vuepress/
│   ├── client.js                                     ← 注册组件
│   └── theme/components/
│       └── <tool-name>/                              ← 组件目录（kebab-case）
│           └── <ToolName>.vue                        ← Vue 3 SFC 组件
└── tools/
    └── <tool-name>.md                                ← Markdown 入口（frontmatter 嵌入组件）
```

| 文件 | 作用 | 必改 |
|---|---|---|
| `<ToolName>.vue` | 组件本体：UI、状态、算法、交互 | ✅ |
| `client.js` | 在 `enhance({ app })` 里 `app.component(...)` | ✅ |
| `<tool-name>.md` | Markdown 入口，frontmatter 启用 `home: true` + `config: custom`，里面写组件标签 | ✅ |
| `tools/README.md` | 总览页加一行链接 | ✅ |

---

## 2. 实战案例：身份证解析工具

以本次新增的 `idcard-parser` 为完整范例，列出实际做了什么。

### 2.1 组件：`<ToolName>.vue`

放在 `docs/.vuepress/theme/components/idcard-parser/IdCardParser.vue`。

```vue
<script setup lang="ts">
import { ref, computed } from "vue";

// 1) 常量 / 字典（如校验权重、省份映射等纯算法数据）
// 2) 类型定义（interface）
// 3) 工具方法（纯函数）
// 4) 响应式状态（ref / reactive / computed）
// 5) 事件方法（按钮点击、表单提交等）
</script>

<template>
  <div class="tool-container">
    <div class="tool-section"> ... 输入区 ... </div>
    <div v-if="..." class="tool-section"> ... 结果区 ... </div>
  </div>
</template>

<style scoped>
/* 仅本组件私有样式，复用全局 CSS 变量，不污染全局 */
</style>
```

实际做了：

- **算法部分**（script）：身份证 18 位加权校验（权重 `7 9 10 5 8 4 2 1 6 3 7 9 10 5 8 4 2`、校验码表 `1 0 X 9 8 7 6 5 4 3 2`）、15 位旧版兼容、GB/T 2260 前缀匹配地区、生日 / 年龄 / 性别推导。
- **状态部分**：`idInput` 输入、`rows` 派生（拆分多行 ID）、`validCount` / `invalidCount` 统计、`csvText` 派生（一键复制用）。
- **交互**：`fillSamples` 填入示例、`clearAll` 清空、`copyText` 复制反馈。
- **没有**用到 `window`/`document`/定时器，所以不需要 `<ClientOnly>` 包裹。

### 2.2 注册：`client.js`

```diff
 import BpmToolComponent from "./theme/components/bpm-tool/BpmTool.vue";
+import IdCardParserComponent from "./theme/components/idcard-parser/IdCardParser.vue";
 ...
 app.component("BpmToolComponent", BpmToolComponent);
+app.component("IdCardParserComponent", IdCardParserComponent);
```

⚠️ 注意 `client.js` 是注册全局组件的**唯一入口**，漏掉这一步 markdown 里写 `<IdCardParserComponent />` 会报"未注册"。

### 2.3 Markdown 入口：`<tool-name>.md`

`docs/tools/idcard-parser.md`：

```markdown
---
home: true
title: 身份证解析
navbar: false
config:
  -
    type: custom
createTime: 2026/09/22 09:30:00
---
<IdCardParserComponent />
```

要点：

| 字段 | 作用 |
|---|---|
| `home: true` + `config: [{ type: custom }]` | 启用 plume 的"自定义首页"模式：去掉默认文档布局，整个页面就是组件 |
| `navbar: false` | 工具页通常更聚焦，隐藏顶导；保留也无妨 |
| `createTime` | 显示在页面上（plume 自动按 git log 也可补） |
| 组件标签 | 必须与 `client.js` 注册名一致 |

### 2.4 工具总览更新：`tools/README.md`

在 "可用工具" 列表下加一行：

```diff
 - **<a href="/tools/bpm">BPM 工具</a>** - 跟着节拍轻点，测节奏速度与稳定性
+- **<a href="/tools/idcard-parser">身份证解析</a>** - 一行一个，校验 + 解析地区 / 出生日期 / 性别 / 年龄
```

⚠️ 别忘了这一步，否则用户从首页进"网站工具"看不到新工具。

---

## 3. 复用步骤模板（5 步加一个新工具）

> 假设工具名为 `foo`，组件名为 `Foo`，新依赖 `xxx`。

### Step 1：建组件文件

```sh
mkdir -p docs/.vuepress/theme/components/foo
```

`docs/.vuepress/theme/components/foo/Foo.vue`：

```vue
<script setup lang="ts">
// 状态、算法、computed、方法
</script>

<template>
  <div class="foo-container">
    <!-- 你的 UI -->
  </div>
</template>

<style scoped>
/* 私有样式 */
</style>
```

> 命名建议：组件文件名 PascalCase，目录 kebab-case。

### Step 2：注册到 `client.js`

```diff
+import FooComponent from "./theme/components/foo/Foo.vue";
 ...
+app.component("FooComponent", FooComponent);
```

### Step 3：建 Markdown 入口

`docs/tools/foo.md`：

```markdown
---
home: true
title: Foo 工具
navbar: false
config:
  -
    type: custom
createTime: 2026/MM/DD HH:mm:ss
---
<FooComponent />
```

### Step 4：在 `tools/README.md` 加链接

```diff
+- **<a href="/tools/foo">Foo 工具</a>** - 一句话功能描述
```

### Step 5：提交推送

```sh
git add docs/.vuepress/client.js \
        docs/tools/README.md \
        docs/tools/foo.md \
        docs/.vuepress/theme/components/foo/
git commit -m "feat: 新增 Foo 工具

一句话描述：做什么、怎么用。

Co-Authored-By: Claude <noreply@anthropic.com>"
git push origin main
```

> Actions 自动构建并部署，**不需要本地 build**。如需确认上线：`curl https://blog.minjor.com/tools/foo`。

---

## 4. 样式约定（怎么跟现有工具保持一致）

参考现有 5 个工具（`accounting/Stat.vue`、`ip-lookup/IPLookup.vue`、`crypto-tool/CryptoTool.vue`、`time-tool/TimeTool.vue`、`bpm-tool/BpmTool.vue`）的样式，新组件应当复用相同的设计 token。

### 4.1 全局 CSS 变量（直接用，不要硬编码颜色）

定义在 `docs/.vuepress/theme/styles/custom.css`（**当前未启用**，仅作未来参考）：

```css
:root {
  --vp-c-brand-1: #5086a1;       /* 主色 */
  --vp-c-brand-2: #6aa1b7;       /* 主色 hover */
  --vp-c-brand-3: #8cccd5;       /* 主色辅助 */
  --vp-c-brand-soft: rgba(131, 208, 218, 0.314);
  --vp-c-bg: #fff;
  --vp-c-bg-alt: #f6f6f7;
  --vp-c-bg-soft: #f6f6f7;
  --vp-c-text-1: rgba(60, 60, 67);
  --vp-c-text-2: rgba(60, 60, 67, 0.78);
  --vp-c-text-3: rgba(60, 60, 67, 0.56);
}
[data-theme="dark"] {
  /* 深色模式由 plume 内置变量控制，不需要手写 */
}
```

### 4.2 通用模式（直接复制这套骨架）

```vue
<style scoped>
.tool-container {
  max-width: 800px;             /* 工具宽度，建议 600-1000px */
  margin: 0 auto;
  padding: 20px;
}

.tool-section {
  margin-bottom: 24px;
  padding: 20px;
  background: var(--vp-c-bg-soft);
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
}

.tool-section h2 {
  margin: 0 0 12px 0;
  font-size: 16px;
  color: var(--vp-c-brand-1);
}

/* 输入框 */
.tool-input {
  flex: 1;
  padding: 10px 14px;
  font-size: 14px;
  border: 2px solid var(--vp-c-brand-1);
  border-radius: 8px;
  background: var(--vp-c-bg);
  color: var(--vp-c-text-1);
  outline: none;
  font-family: monospace;       /* 输入类工具建议 monospace */
  transition: border-color 0.2s;
}
.tool-input:focus { border-color: var(--vp-c-brand-2); }

/* 主按钮 */
.tool-btn {
  padding: 10px 20px;
  font-size: 14px;
  font-weight: 600;
  background: var(--vp-c-brand-1);
  color: white;
  border: none;
  border-radius: 8px;
  cursor: pointer;
  transition: background 0.2s;
  white-space: nowrap;
}
.tool-btn:hover { background: var(--vp-c-brand-2); }
.tool-btn:disabled { opacity: 0.5; cursor: not-allowed; }

/* 复制按钮（小尺寸） */
.copy-btn {
  background: none;
  border: 1px solid var(--vp-c-divider);
  border-radius: 4px;
  cursor: pointer;
  padding: 2px 6px;
  font-size: 12px;
  color: var(--vp-c-text-2);
  transition: all 0.2s;
}
.copy-btn:hover,
.copy-btn.copied {
  background: var(--vp-c-brand-soft);
  border-color: var(--vp-c-brand-1);
  color: var(--vp-c-brand-1);
}

/* 错误提示 */
.error-message {
  color: #e53935;
  font-size: 14px;
  margin-top: 8px;
}

/* 响应式 */
@media (max-width: 640px) {
  .tool-container { padding: 12px; }
  .tool-section { padding: 16px; }
}
</style>
```

### 4.3 表格（参考 `Stat.vue` 的 `.transaction-table`）

```css
.result-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 13px;
  background: var(--vp-c-bg);
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
}
.result-table th,
.result-table td {
  padding: 10px 12px;
  text-align: left;
  border-bottom: 1px solid var(--vp-c-bg-alt);
  white-space: nowrap;
}
.result-table th {
  background: var(--vp-c-brand-soft);
  color: var(--vp-c-brand-1);
  font-weight: 600;
  font-size: 12px;
}
.result-table tbody tr:hover { background: var(--vp-c-bg-alt); }

/* 表格外层包裹（移动端横向滚动） */
.table-wrap { width: 100%; overflow-x: auto; }
```

### 4.4 徽章 / 标签

```css
.badge {
  display: inline-block;
  padding: 2px 10px;
  border-radius: 12px;
  font-size: 12px;
  font-weight: 600;
}
.badge-ok   { background: rgba(46, 125, 50, 0.12); color: #2e7d32; }
.badge-err  { background: rgba(198, 40, 40, 0.12); color: #c62828; }
.badge-warn { background: rgba(245, 124, 0, 0.12); color: #e65100; }
```

---

## 5. 常见需求与处理

### 5.1 需要访问 `window` / `document` / 定时器

VuePress 默认会做 SSR（构建时也会预渲染），顶层访问 `window` 会报 `ReferenceError: window is not defined`。

**两种处理：**

A. 把浏览器 API 放在 `onMounted` / 事件回调里（推荐）。例：`time-tool/TimeTool.vue` 用 `setInterval` 在 `onMounted` 里启动，`onUnmounted` 里清理。

B. 用 `<ClientOnly>` 包裹整个组件标签。例：`tools/time.md`、`tools/bpm.md`：

```markdown
<ClientOnly>
<FooComponent />
</ClientOnly>
```

> 选 A 更优雅（避免 SSR 输出空壳），但需要把状态初始化延迟到 `onMounted`。

### 5.2 需要图表

直接用 `echarts`（已在 `dependencies` 里）。在组件里 `import * as echarts from 'echarts'`，DOM ready 后 `echarts.init(dom)`，watch 响应式数据变化时 `setOption`。销毁时 `dispose()`。

> 参考 `accounting/Stat.vue` 的图表用法。

### 5.3 需要加密 / 哈希

项目已装：

- `sm4-crypto` — 国密 SM4 对称加解密
- `blueimp-md5` — MD5 摘要

直接 `import` 用，不需要额外装包。参考 `crypto-tool/CryptoTool.vue`。

### 5.4 数据需要持久化

localStorage 即可，不引入额外依赖。命名加前缀避免冲突：

```ts
const KEY = "minjor:foo:data";
function load() { try { return JSON.parse(localStorage.getItem(KEY) || "[]"); } catch { return []; } }
function save(d) { localStorage.setItem(KEY, JSON.stringify(d)); }
```

### 5.5 需要引入新依赖

1. `npm i <pkg>` → 写入 `package.json` + `package-lock.json`
2. 提交时**只动** `package.json` / `package-lock.json` / 新增的组件文件，不要夹带其他改动
3. 在新组件顶部注释里说明为什么需要它

---

## 6. 验证清单（提交前自检）

- [ ] **注册**：在 `client.js` 里 `app.component(...)` 添加了
- [ ] **入口**：`<tool-name>.md` 的 frontmatter 含 `home: true` + `config: [{ type: custom }]`，组件标签与注册名一致
- [ ] **总览**：`tools/README.md` 加了新工具的链接
- [ ] **样式**：复用全局 CSS 变量，没硬编码颜色；移动端宽度下不溢出
- [ ] **暗色**：切到深色模式（plume 自动），文字 / 背景对比度足够
- [ ] **SSR**：如果用了 `window`，确认用 `onMounted` 或 `<ClientOnly>` 包了
- [ ] **commit**：信息用 `feat:` 前缀，附功能描述

> **不**在本地跑 `npm run docs:build`——本仓库 push main 即自动部署。验证上线用 `curl https://blog.minjor.com/tools/<tool-name>`。

---

## 7. 易踩的坑

| 现象 | 原因 / 解决 |
|---|---|
| markdown 里写 `<XxxComponent />` 报"未注册" | 漏了 `client.js` 注册 |
| 页面变成空白 / 默认文档布局 | frontmatter 漏 `home: true` + `config: [{ type: custom }]` |
| 构建报 `window is not defined` | 顶层用了浏览器 API；移到 `onMounted` 或加 `<ClientOnly>` |
| 工具在总览页找不到 | `tools/README.md` 漏加链接 |
| `navigator.clipboard` 报错 | 只在 HTTPS / localhost 下可用；非 HTTPS 环境降级用 `document.execCommand('copy')` |
| 组件名跟目录名不一致容易乱 | 约定：目录 `kebab-case`（如 `ip-lookup/`），组件 `PascalCase`（如 `IPLookup.vue`），注册名 `PascalCase` + `Component` 后缀（如 `IPLookupComponent`） |

---

## 8. 命名与代码风格约定

- **目录**：`kebab-case`，如 `idcard-parser/`、`time-tool/`
- **组件文件**：`PascalCase.vue`，如 `IdCardParser.vue`、`TimeTool.vue`
- **注册名**：`PascalCase` + `Component` 后缀，如 `IdCardParserComponent`
- **Markdown 文件**：`kebab-case.md`，路径与目录对应，如 `tools/idcard-parser.md` → URL `/tools/idcard-parser/`
- **CSS 类名**：`kebab-case`，**全部** scope 在组件内（`<style scoped>`），不污染全局
- **TypeScript**：组件用 `<script setup lang="ts">`，类型显式声明，不要 `any` 滥用

---

## 9. 提交信息模板

```text
feat: 新增 <工具名> 工具

<一句话功能描述>。
<支持的输入 / 操作方式>。
<主要输出>。

Co-Authored-By: Claude <noreply@anthropic.com>
```

修复 / 微调用：

```text
fix(<scope>): <一句话>

如：fix(idcard-parser): 移除生肖、星座字段
```

---

## 10. 完成后的最后一步

提交 → push → 等待几秒 → `curl https://blog.minjor.com/tools/<tool-name>` 确认 200。

不用本地 build，不用看 gh-pages，不用手动部署。详细见 [架构文档.md](架构文档.md) 第 3 节及 `memory/deployment.md`。